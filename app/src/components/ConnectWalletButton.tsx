import * as React from "react";

export const ConnectWalletButton: React.FC = () => {
  return (
    <button
      className="overflow-hidden self-end px-10 py-5 text-2xl bg-purple-600 rounded-[34px] max-md:px-5"
      onClick={() => {}}
      aria-label="Connect wallet"
    >
      CONNECT WALLET
    </button>
  );
};
